<h3><img src="{{user.pictureUrl}}" alt="" style="width: 4rem;border-radius: 50%;"></h3>
<h3>{{user.username}}</h3>
{{#each statsPerLeague}}
    <div class="card" style="width: 20rem;">
        <img src="{{countPerLeague._id.imageUrl}}" alt="{{name}} image">
        <div class="card-body">
            <h3 class="card-title">{{countPerLeague._id.name}}</h3>
            <h6 class="card-subtitle">Members: {{countPerLeague._id.members.length}}x👤</h6>
            <h6 class="card-subtitle">Challenges: {{countPerLeague.count}}x🎲</h6>
            <h6 class="card-subtitle">Stake: {{countPerLeague.totalStake}}x💰</h6>
            <h4 class="card-text">Personal</h4>
            <p class="card-text">{{#if countPerWinner}}{{countPerWinner.count}}{{else}}0{{/if}}x🎲 winned out of {{countPerUser.count}}x🎲</p>
            <p class="card-text"">{{#if countPerWinner}}{{countPerWinner.totalStake}}{{else}}0{{/if}}x💰 earned out of {{countPerUser.totalStake}}x💰</p>
            <h4 class="card-text">Leader board</h4>
            <ol>
            {{#each rankingPerLeague}}
                <li><img src="{{_id.winners.pictureUrl}}" alt="" style="width: 3rem;border-radius: 50%;">{{_id.winners.username}} ({{totalStake}} x 💰| {{count}} x 🎲 | <a href="/boards/{{_id.winners._id}}">see stats</a>)</li>
            {{/each}}
            {{#each noWinners}}
                <li><img src="{{pictureUrl}}" alt="" style="width: 3rem;border-radius: 50%;">{{username}}  (0 x 💰| 0  x 🎲 | <a href="/boards/{{_id}}">see stats</a>)</li>
            {{/each}}
            </ol>
        </div>
    </div>
{{/each}}